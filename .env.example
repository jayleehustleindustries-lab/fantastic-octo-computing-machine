# Supabase project (server-side only — never exposed to the browser)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Simple shared password gating the whole app (single-user personal tool, no real auth system)
APP_PASSWORD=
